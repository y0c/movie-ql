type Movie {
  id: Int!
  title: String!
  year: String!
  runtime: String!
}
type Query {
  movies: [Movie]!
  movie(id: Int!): Movie
}

type Mutation {
  addMovie(title: String!, year: String!, runtime: String!): Movie!
  removeMovie(id: Int!): Boolean!
}
